int add(int x, int y){
    int add = x + y;
    return add;
}
int diff(int x, int y){
    int diff;
    if(x>y){
        diff = x - y;
    }else{
        diff = y - x;
    }
    return diff;
}